# proto-relocation-interface-unify

Issue: #70 (P0) · Branch: `feat/issue-70-daemon-stateless-config-proto` · DAG: `issue-70-p0-proto-relocation`

把根 `proto/` 迁入 `crates/theway-transport/proto/`，并统一 gRPC / JSON-RPC / MCP 到同一契约层。
