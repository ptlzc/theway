你是 E2E 测试代理。当前目录是隔离测试目录。请**依次**调用以下 8 个自动化工具并逐项打标记（每完成一个用 bash 执行 `echo <工具名>_ok >> marks.txt`，标记用小写去掉下划线：new_cron_job→newcronjob_ok、list_cron_jobs→listcronjobs_ok、remove_cron_job→removecronjob_ok、set_cron_job_state→setcronjobstate_ok、new_trigger→newtrigger_ok、list_triggers→listtriggers_ok、remove_trigger→removetrigger_ok、set_trigger_state→settriggerstate_ok）：

1. new_cron_job — 创建一个 cron 任务（名称 e2e-cron，命令 echo ok，schedule "* * * * *"。注意：不要等待/搜索 cron 输出文件，禁止 find / 和全盘搜索）
2. list_cron_jobs — 列出 cron 任务，确认 e2e-cron 存在
3. set_cron_job_state — 把 e2e-cron 暂停（enabled=false）。禁止用 find 搜索任何文件，只调用工具本身
4. set_cron_job_state — 再恢复 e2e-cron（enabled=true），然后 remove_cron_job 删除它
5. new_trigger — 创建一个动态 trigger（名称 e2e-trigger，匹配规则任意合理定义，如文件变化），list_triggers 确认存在
6. set_trigger_state — 暂停 e2e-trigger（enabled=false），再恢复（enabled=true）
7. remove_trigger — 删除 e2e-trigger

要求：8 个工具全部调用成功（set_cron_job_state 调用两次，标记只打一次 setcronjobstate_ok 即可）；每个工具成功后立即追加标记；全部完成后用一句话汇报。
