你是 E2E 测试代理。当前目录是隔离测试目录。请**依次**调用以下 2 个网络工具并逐项打标记（每完成一个用 bash 执行 `echo <工具名>_ok >> marks.txt`）：

1. web_fetch — 抓取 https://example.com 页面，确认返回内容非空（提取标题或正文片段）
2. web_search — 搜索 "theway agent" 或 "rust agent framework"，确认返回结果列表

要求：2 个工具全部调用成功；每个成功后立即追加标记；如果某个工具因网络失败，重试一次；全部完成后用一句话汇报。
