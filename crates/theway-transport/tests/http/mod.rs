//! HTTP transport tests (docs/rust-test-files.md): the router e2e in `endpoints`, unit
//! helpers in `helpers`.

mod coverage;
mod endpoints;
mod extensions;
mod helpers;
mod sessions;
mod state;
mod tools;
