//! Bridge entry for the `agent_loop` integration-test suite, split by test domain into
//! `tests/agent_loop/` (helpers / lifecycle / updates / permission).

#[path = "agent_loop/mod.rs"]
mod agent_loop;
