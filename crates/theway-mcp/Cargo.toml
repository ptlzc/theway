[package]
name = "theway-mcp"
version = "0.75.0"
edition = "2024"
rust-version = "1.85"
description = "Minimal MCP (Model Context Protocol) stdio client for the theway agent runtime."
license = "MIT"
authors = ["theway contributors"]

[dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync", "time", "io-util", "io-std", "process"] }
tokio-util = { version = "0.7", features = ["io"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
async-trait = "0.1"
thiserror = "2"
parking_lot = "0.12"
futures = "0.3"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "stream"] }

[dev-dependencies]
anyhow = "1"
axum = "0.8"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "test-util"] }

[lints]
workspace = true
