# Archived Spec: Session-Scoped Public Webhook Endpoint

Status: archived and not implementation-ready.

This design depended on the removed public cross-agent service. It is kept only as historical
context and must not be used to drive implementation.

Future public-ingress work needs a new design that is independent of the removed service and has a
fresh threat model, storage model, abuse controls, CI strategy, and production release gate.
